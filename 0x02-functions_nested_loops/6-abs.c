#include "main.h"

/**
 * _abs - gets the absolute value of an integer
 * @int: integer to be checked
 *
 * Return: the value of a
 */
int _abs(int a)
{
	if (a < 0)
	{
		return (a * -1);
	}
	else
	{
		return (a);
	}
	return (a);
}
