#include <stdio.h>

/**
 * main - printf message to console
 * @void: return type of main
 *
 * Description: uses printf to output msg
 * Return: 0 when execute successfully
 */
int main (void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
